VITE_SANITY_PROJECT_ID=v4vmlsv9
VITE_SANITY_DATASET=production
VITE_SANITY_API_VERSION=2026-04-21

# Google Places API — optional, used to fetch Google reviews for the
# testimonials section. Create a key in Google Cloud Console with
# "Places API" + "Maps JavaScript API" enabled, then restrict it to
# the production domain (HTTP referrers) before exposing to the client.
VITE_GOOGLE_PLACES_API_KEY=
